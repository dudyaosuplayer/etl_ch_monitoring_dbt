version: 2

sources:
  - name: raw
    tables:
      - name: customers
      
models:
  - name: stg_customers
    description: "Staging table for customers"
    columns:
      - name: customer_id
        description: "Unique customer id"
        tests:
          - not_null